{
  "name": "echo-autonomous-builder",
  "version": "1.0.0",
  "description": "Autonomous execution engine — the HANDS of ECHO OMEGA PRIME",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.99.0",
    "@cloudflare/workers-types": "^4.20241127.0",
    "typescript": "^5.7.0"
  }
}
